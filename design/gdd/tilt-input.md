# Tilt Input

> **Status**: Revised twice on 2026-09-21: after the first `/design-review` (MAJOR REVISION NEEDED, 8 blocking) and after the re-review (NEEDS REVISION, 5 blocking); a fresh-session re-review is recommended before approval
> **Author**: user + agents
> **Last Updated**: 2026-09-21
> **Implements Pillar**: Pillar 4 (One-Thumb Simplicity); supports Pillar 2 (Fair but Merciless Difficulty) by keeping sensor noise and sensor loss out of deaths
> **Revision 1, 2026-09-21** (user decisions after the first review): the neutral pose is captured at Menu-to-Play and at `run_resumed`; a sensor lost mid-run pauses the run through a Run State pause source `sensor_lost`; machinery cut (F5 as runtime logic, `UNITS_SUSPECTED`, 9 log codes to 6, 5 states to 3); the "play anywhere" promise scoped to postures where the roll formula works.
> **Revision 2, 2026-09-21** (after the re-review; five specialists, creative-director as senior). *User decisions:* (1) a restart from Hit or Paused inherits the neutral **unless a steady pose far from it shows the phone moved** (conditional re-anchor, rule 7); (2) Pillar 4 was reworded in `game-concept.md` ("in any normal holding posture" instead of "anywhere"); (3) a touch-half **fallback** for phones without a usable sensor is in the MVP (rule 11); (4) signed on-device V-1 evidence blocks the first playable and AC-40 has an owner (Acceptance Criteria, gate policy). *Design decisions taken from the review:* the `sensor_lost` pause is level-triggered in Tilt Input's adapter (no Run State change); position mapping is the working assumption for Ball Movement; a rest pose beyond `PHI_MAX - FS_eff` loses range instead of adding bias (pass-through, `NEUTRAL_LIMIT` removed); the dropout hold counts clamped poll time and at least 3 invalid polls; a pending capture takes the median of `N_min` samples; Platform Services owns the app-lifecycle notification; `SENSOR_SIGN` is per platform; the axes are screen-relative in every orientation (the earlier "axes flip" claim was wrong); the acceptance suite got a defined fixture and corrected ACs. Engine facts were checked against the 4.7.2 source excerpts (godot-specialist); items marked UNVERIFIED were not.

## Overview

Tilt Input is the Core-layer system that turns the phone's physical tilt into the single steering signal the whole game is played with. Every frame it reads the device's gravity vector, converts it into a signed left-right tilt for the game's portrait screen, removes the player's neutral pose, filters out noise and hand tremor, and publishes one steering value plus a validity flag. It owns everything between the sensor and that value: the sensor source and its availability, the axis and sign mapping, neutral-pose capture, dead zone, smoothing and sensitivity, the effect of the app going to the background on its own state, a fallback input (arrow keys in development, a touch half-screen hold for phones without a usable sensor), and the signal that tells the game the sensor was lost. It captures the neutral where the pre-tap pose is a genuine rest pose (Menu-to-Play and `run_resumed`) and keeps it across restarts unless a steady, clearly different pose shows the phone moved. It does not own how the ball moves: Ball Movement turns the steering value into motion around the tube, and no other system reads the sensor. Because tilt is the game's only control, a wrong sign, a wrong neutral or a noisy sample is felt by the player as an unfair death (Pillar 2), and the phone must steer correctly in the postures the game supports (Pillar 4); players should feel their hand *is* the ball. Two technical decisions live outside this GDD and become ADRs, neither written yet: which loop polls the sensor (shared with the game-loop ADR that Run State also needs) and which sensor source is used (`get_gravity()` is the working choice, with the accelerometer as a fallback), both settled after the on-device spike. The published value is a normalized steering value; **the working assumption is that Ball Movement maps it to the ball's angular position** (game concept, Core Mechanic 2; technical preferences), with a rate mapping as the alternative the spike compares (Open Question 10).

## Player Fantasy

**The fantasy.** Your hand is the ball. The player holds the phone, tips it, and the ball answers right now, with no setup, no calibration screen and no sense of a sensor in between. It is the only control in the game (*Pillar 4: "Controls use a single input axis (left-right around the tube); the game is playable one-handed in any normal holding posture, seated, standing, reclined or commuting."*), so the whole "flawless reflexes" fantasy of the concept rests on it: the player must feel their reactions getting sharper, and never that the phone got in the way.

**What it feels like.** Tilting is continuous and analog: the ball's angle follows the phone's tilt, a small tip moves it a little, a big tip moves it far, and holding still holds the ball still. The game adopts the pose the player is holding when they press Play (and when they come back from a pause), and it remembers it between restarts: restarting is a tap, not a chore, and the ball starts still when the phone is held the way it was held before. If the phone was clearly moved to a different resting pose while the run was over, the next restart adopts that pose instead. (The steadiness of the first second also rests on the hazard-free start that Pattern & Difficulty owes; see Open Question 11.)

**Where the promise holds.** Seated, standing, reclined against a backrest, or in a steady vehicle. It is **not** promised for lying flat on the back or on one's side, or for a vehicle that is braking or cornering hard: the roll formula cannot represent those postures (F1), and how far the OS-fused gravity vector resists vehicle acceleration is unmeasured (spike). A rest pose beyond `PHI_MAX - FS_eff` costs range on one side but never adds a steady bias (F2). A wrist twist at high pitch also loses gain: at `FS` 25 full lock by twist alone is unreachable above about 65 deg of pitch (F1). Section B is scoped to what the spike can confirm; a wider promise needs the F1 rework named in Open Question 6.

**Serves the pillars.** *Pillar 2, "every death traces back to the player's own choice or reaction, never to randomness or bad luck"*: sensor noise, a wrong neutral, a swapped direction or a lost sensor must never be the cause of a death (a lost sensor pauses the run, and a run never starts on a dead sensor). Pillar 4 as quoted above.

**When feelings conflict.** Smoothing removes hand tremor but adds lag. Fairness comes first: a steady signal beats a raw one, but any lag the filter adds must stay under what the player can feel (a budget set on a device). "Never fights me" ranks above "my hand is the ball" only when the two cannot both be met.

**Feelings to avoid.** Sluggish or rubber-band steering; a neutral that creeps or that was taken from a moment the player did not choose (right after dying); left and right swapped; a lurch in the first frames because the tap that started the run jolted the phone; an awkward posture forced on the player; a calibration chore between runs; a control that goes dead while the run keeps going.

**Reference anchors (not measured, hypotheses):** Doodle Jump (continuous tilt with no calibration step), tilt-maze games (physical feel, inertia), Temple Run (auto-run plus tilt). None was tested against this game; feel is validated on a device (spike).

## Detailed Design

### Core Rules

1. **One owner of the sensors.** Only Tilt Input reads motion sensors (`Input.get_gravity()` and its siblings). No other system does; a CI grep lint enforces it (AC-37).
2. **Output contract.** Tilt Input publishes, pulled by consumers (no per-frame signal; reading a getter twice without a poll gives the same value): `steer`, a finite float in `[-1, 1]`; `valid`, a bool; and `input_source` (`SENSOR` or `FALLBACK`). **`steer > 0` when the right edge of the phone, as the player sees it in portrait, is lowered**; Ball Movement moves `theta` in the positive direction for `steer > 0` (Tube Track: tilting right increases theta). `steer` is never NaN or infinite: a bad value becomes 0, the filter state resets to 0, and one `BAD_OUTPUT` error is logged. In Acquiring and Unavailable, `valid` is false and `steer` is 0. While a neutral capture is pending (rule 8) `steer` is 0. Consumers ignore `steer` outside Running.
3. **Orientation.** The game is **portrait, locked** by design (the camera, the HUD and the art bible assume it): `display/window/handheld/orientation` must be portrait (value 1; the default 0 is landscape and does not swap the viewport size; the enum value is UNVERIFIED locally because `project.godot` does not exist yet, confirm it in the editor). Platform Services owns the setting and a CI project-settings lint checks it (AC-37). Godot reports gravity in **screen-relative axes in every orientation** (verified in the 4.7.2 source: iOS rotates the CoreMotion gravity into screen coordinates for portrait, upside-down and both landscapes; Android negates or swaps x and y for `ROTATION_90/180/270`), so in portrait the steering axis is the device x axis with no remap, and the sign does not flip in reverse portrait. `NOT_PORTRAIT` is therefore a **diagnostic** (the game was not built for that orientation), not a correctness guard. The tilt is how much the right edge is lower than the left, measured from gravity (F1). On a natural-landscape Android device (tablet, foldable) a portrait screen is a rotated display, and the same screen-relative output applies (UNVERIFIED on hardware).
4. **Sensor source and samples.** The working source is `Input.get_gravity()`, an OS-level virtual sensor (iOS `deviceMotion.gravity`, Android `TYPE_GRAVITY`); the sensor-source ADR settles it, with the accelerometer as a fallback where `TYPE_GRAVITY` is absent. Units are m/s^2 on both platforms (verified: iOS multiplies CoreMotion's g by 9.80665). The API has **no per-sample timestamp and no freshness flag**: it returns the last value the OS delivered. So a "sample" in this GDD is one poll-time read stamped with the poll clock; consecutive samples may repeat the same sensor value (Android delivers about 50 Hz at `SENSOR_DELAY_GAME`, iOS refreshes once per rendered frame), and every count of samples counts frames. A sample is valid if the vector is finite and `|g| >= G_MIN`; a zero vector means no such sensor or a non-mobile platform. **`SENSOR_SIGN` (+1 or -1) is per platform** and chosen by feature tag (`ios` or `android`): iOS gravity reads about (0, -g, 0) with the phone upright, Android `TYPE_GRAVITY` follows the accelerometer convention (about (0, +g, 0), from the SDK docs, UNVERIFIED locally), so the expected values are **iOS +1, Android -1**, and V-1 confirms them. F1 uses only `g.x`.
5. **Pipeline, in this order:** validity check, roll angle (F1), subtract neutral, low-pass filter (F3), then dead zone, scale by `FS_eff`, clamp to `[-1, 1]` and response curve as one remap (F4). The dead zone acts on the filtered signal, so noise does not flicker across its edge.
6. **Polling.** `poll()` takes no argument. It is called **once per rendered frame, in every phase (Menu, Running, Paused, Hit), by exactly one caller, before Ball Movement steps** (order fixed in the game-loop ADR; the recommended shape is one driver node that calls `tilt.poll()`, then Run State's tick, then the ball step, in one `_process` callback, not relying on `process_priority`; Run State already requires `_process` for its tick, Run State Open Question 4). The driver needs `process_mode = PROCESS_MODE_ALWAYS` because it polls while Paused. Polling from `_physics_process` is **not supported**: catch-up physics ticks carry wall-clock stamps microseconds apart, and the equal-stamp rule below would discard them. Time comes from the injected clock (microseconds, the same clock as Run State's `press_us`): `dt = clamp((now - previous_now) / 1e6, 0, DT_MAX)` with the shared `DT_MAX` 0.1 s. `previous_now` is the stamp of the last **accepted** poll (any poll whose stamp is greater than it, whether its sample was valid or not); it starts as "none", so the first poll of a fresh core, even one stamped 0, is accepted, has `dt = 0` and appends its sample if valid. A poll whose stamp is not greater than `previous_now` (equal or going backwards) appends no sample, uses `dt = 0` and leaves `previous_now` unchanged. `dt` is not taken from the engine's frame delta (it is scaled by `Engine.time_scale` and capped by the engine). Note: 4.5 moved physics interpolation into the scene tree and iOS can run 120 Hz frames (two `_process` calls per physics tick); the game-loop ADR decides how the ball moves relative to that, and Tilt Input only requires the poll on the render frame.
7. **Neutral capture policy.** The neutral `phi0` is the roll angle the player holds as "level". Three events matter, from the adapter (Run State's `phase_changed` supplies the previous phase of a reset; provisional):
   - **Always capture** on `run_reset` whose previous phase was Boot or Menu (the player pressed Play: a genuine rest pose) and on `run_resumed` (the end of the resume countdown, when the player has re-gripped the phone).
   - **`run_reset` from Hit or Paused (a restart):** capture like the events above if `neutral_stale` is true; otherwise **conditional re-anchor**: evaluate the pre-tap window (F2) and recapture only if it holds at least `NEUTRAL_MIN_SAMPLES` samples, its spread is at most `REANCHOR_SPREAD` and its median differs from the current `phi0` by more than `REANCHOR_FRACTION * FS_eff`; otherwise the previous `phi0` is **inherited** unchanged and the filter keeps running.
   Reason: before a restart the pose is the death pose or the pause pose, often not a rest pose, and the window falls inside the frozen Hit phase where the player gets no feedback, so inheriting is the default; but a phone that was put down or moved to another posture leaves a steady, distant pose, which a silent permanent bias would punish (Pillar 2). **Known risk (Open Question 22):** under a position mapping a steady large tilt is normal play, so a death while holding one can look like a new rest pose. The neutral is otherwise **fixed** (no continuous recentering, no manual recalibration in the MVP). `neutral_stale` is true at construction, set when the app is backgrounded (rule 9) or a sensor is lost (rule 10), and cleared by any capture.
8. **Capture mechanics.** Tilt Input keeps a ring buffer of roll angles stamped with the injected clock: capacity 256, a fixed `BUFFER_AGE` of 1.0 s, oldest overwritten, samples older than `BUFFER_AGE` dropped at every append and at every capture. **All durations in seconds (`G`, `W`, `DROPOUT_HOLD`, `SENSOR_RESUME_SETTLE`, `SENSOR_START_TIMEOUT`, `BUFFER_AGE`) are converted once at load with `roundi(x * 1e6)` and every comparison runs on integer microseconds**, so a closed window cannot be off by one from float rounding. A capture sets `phi0` to the **median of the samples in `[t - G - W, t - G]`** (F2), where `t` is the handler's time; the guard skips the jolt of the tap that caused the capture. If fewer than `NEUTRAL_MIN_SAMPLES` fall in the window, or Tilt Input is not Live, `neutral_pending` is set (a conditional re-anchor with too few samples does **not** set pending: it inherits). A **pending capture takes the median of the first `NEUTRAL_MIN_SAMPLES` valid samples after the event** (so an outlier or a one-frame jolt does not decide it) and `steer` is 0 until then. `phi0` is clamped to `+-PHI_MAX` (F2). After a capture the filter starts at 0 (the relative angle is about 0 by construction, and the jolt of the tap is filtered instead of going straight into `steer`). On entering Live from Acquiring or Unavailable without a capture, the filter starts at the current relative angle. The settling tick (Run State rule 14) only gives the filter extra polls; nothing depends on it.
9. **Availability and the app lifecycle.** At boot, on **Android**, the node reads `ProjectSettings` (`input_devices/sensors/enable_gravity`, `enable_accelerometer`; both default false and require a restart) and passes `sensors_enabled` to the core; if the chosen sensor is disabled it logs `SENSORS_DISABLED`. On **iOS** the CoreMotion manager starts unconditionally (verified: `godot_commonInit`), so `sensors_enabled` is true there. **Platform Services owns the OS notification** (`NOTIFICATION_APPLICATION_PAUSED` / `_RESUMED`) and exposes two signals, provisionally `app_backgrounded` and `app_foregrounded` (Open Question 24); Tilt Input's adapter connects them to `on_app_backgrounded()` and `on_app_foregrounded()` on the core, so one node owns one notification and there is no tree-order dependency. On backgrounded: the buffer is cleared, `neutral_stale` is set and the state returns to Acquiring; in Acquiring the buffer is still cleared and the settle restarted, with no state change and no signal. On foregrounded: samples are discarded for `SENSOR_RESUME_SETTLE`, because Android unregisters the listener when the app pauses (verified) while Godot keeps the last vector, which would pass the validity check with fresh poll stamps. On iOS `NOTIFICATION_APPLICATION_PAUSED` fires only when the app enters the background; the control centre, the notification shade and the app switcher fire only focus-out, which stops rendering while CoreMotion keeps running and is re-read live, so no stale vector arises there (verified). Delivery of the notification to a plain Node is UNVERIFIED (Platform Services confirms it).
10. **Sensor loss and the pause.** While Live with `SENSOR`, invalid polls are counted by F6: `steer` holds its last value while the accumulated clamped poll time of consecutive invalid polls is at most `DROPOUT_HOLD` **or** fewer than `DROPOUT_MIN_POLLS` (3) polls have been invalid (`valid` stays true, so a one-frame blip or a frame hitch is invisible). After that the state is Unavailable: `valid` false, `steer` 0, `neutral_stale` set, `availability_changed(false)` emitted. A valid sample brings the state back to Live (`availability_changed(true)`) and the next capture event takes a fresh neutral. **The pause is level-triggered, not edge-triggered.** The thin adapter, owned by Tilt Input, requests `pause_requested(sensor_lost)` (a Run State pause source, already defined) whenever `valid` is false **and the phase is Running or Resuming**, and it evaluates that on every entry into those phases (including on `run_started`) as well as on every `availability_changed(false)`. So a run that would start with `valid` false (a restart from Hit after the sensor dropped, or after the app was backgrounded in Hit, or a Restart from the sensor-lost pause screen) is paused on its first tick (Run State accepts a pause on the settling tick), and a run is never played on with a dead control. The pure function `sensor_lost_pause_needed(phase, valid)` holds the rule (AC-43). The pause screen must always offer a way to the Menu, so the state is never a dead end; Resume and Play stay disabled while `valid` is false (UI Requirements).
11. **Fallback input.** `input_source` becomes `FALLBACK` (a) at construction in a debug build with sensors disabled, and (b) when Acquiring times out (no valid sample within `SENSOR_START_TIMEOUT`, counted from the first poll after the settle), **in any build**. In FALLBACK the state is Live, `valid` is true, and `steer` follows a target: `target = clamp(key + touch, -1, 1)`, `steer = move_toward(steer, target, FALLBACK_SLEW * dt)` (F6). `key` comes from `steer_left` / `steer_right` (debug builds only, guarded by `InputMap.has_action()`; the actions must exist in `project.godot`), and `touch` from a **touch half-screen hold forwarded by the HUD / Menus** (left half -1, right half +1, both 0), sent only while the phase is Running and released on leaving Running so it never clashes with tap-anywhere restart. Tilt Input itself reads no touch (rule 13); the core takes a `fallback_source: Callable() -> int` in `{-1, 0, +1}`. FALLBACK is terminal for the session (it is a fallback for phones without a usable sensor and a development aid, not a selectable control scheme), and while `input_source` is `SENSOR` the fallback is ignored. `sensors_enabled = false` in a **release** build is a configuration error, not a device limitation: the state is Unavailable with one `SENSORS_DISABLED` error (the lint in AC-37 keeps it from shipping). Mouse control is post-MVP (game concept).
12. **Testing seam.** `TiltMath` holds the pure functions (F1-F4, `should_reanchor` and `sensor_lost_pause_needed`) with no validation. `TiltCore` is a `RefCounted` with no Node and no `Input` call: it takes an injected `sample_source: Callable() -> Vector3`, a `clock: Callable() -> int`, a `log_sink: Callable(level, code, detail)`, a `fallback_source: Callable() -> int`, `is_debug`, `sensors_enabled`, `is_portrait` and a `TiltConfig` (which carries the resolved `sensor_sign`). Events in: `poll()`, `on_run_reset(previous_phase)`, `on_run_resumed()`, `on_app_backgrounded()`, `on_app_foregrounded()`. `TiltConfig.validated()` applies rule 14; a test-only `TiltConfig.unvalidated()` bypasses it and must not be referenced from `src/` (AC-37). `PHI_MAX`, `FS_EFF_MAX`, `F_MIN`, `BUFFER_AGE`, `TAU_FLOOR` and `DROPOUT_MIN_POLLS` are constants of `TiltMath` / `TiltCore`, not config fields. Log codes (structured): `SENSORS_DISABLED`, `SENSOR_TIMEOUT`, `NOT_PORTRAIT`, `KNOB_CLAMPED`, `BAD_OUTPUT`, `POSTURE_UNSUPPORTED`. The core logs every rejected input; the production sink rate-limits to one message per code per 1.0 s of clock. Read-only getters: `steer`, `valid`, `input_source`, `state`, `phi0`, `phi_f`, `neutral_pending`, `neutral_stale`, `sample_count`. A thin `TiltInput` node reads the sensor, `ProjectSettings` and the fallback keys and forwards them; a constructor given an invalid Callable logs one error and leaves the core Unavailable (guard with `is_valid()` at init). GDScript pitfalls for the seam: Callables are untyped, lambdas capture locals by value (tests mutate a member or a one-element Array), a lambda capturing `self` and stored inside a `RefCounted` creates a leaking cycle (use bound methods or a `dispose()`), `class_name` collisions can occur in headless runs (`preload` the two classes to avoid the class cache), and the ring buffer should be a `PackedFloat64Array` of angles plus a `PackedInt64Array` of stamps with a head index and no getter exposing them (getters return copies). Tests build a fresh `TiltCore` each.
13. **No touch inside Tilt Input.** Tilt Input never reads touch or gestures (Pillar 4); touch belongs to HUD and Menus, which forward the fallback hold through `fallback_source` (rule 11).
14. **Config validation.** At load, in this order: (1) each knob is clamped to its own safe range (a non-finite knob takes its default); `sensor_sign` other than +1 or -1 takes the platform's expected value; (2) if `G + W > 0.9`, `W` is reduced to `0.9 - G`; (3) `SENSOR_RESUME_SETTLE` is clamped to at most `1.0 - G - W`; (4) `NEUTRAL_MIN_SAMPLES` is clamped to at most `floor(W * F_MIN)`; (5) `DEAD_ZONE` is clamped to at most `0.15 * TILT_FULL_SCALE`. One `KNOB_CLAMPED` error per knob changed (`CURVE_EXP < 1` becomes 1; a `FILTER_TAU` of 0 or less becomes 0.02). The bound `SETTLE + G + W <= 1.0` comes from the shortest resume countdown (1.0 s, Run State): after an app pause the buffer is empty, samples are accepted only after `SETTLE`, and the window at `run_resumed` must fit after that.

### States and Transitions

| State | Meaning | `valid` | `steer` |
|-------|---------|---------|---------|
| Acquiring | Sensors enabled, waiting for the first valid sample (at boot, or after the app was backgrounded) | false | 0 |
| Live | Valid samples flowing (`input_source` = `SENSOR`), or the fallback input (`input_source` = `FALLBACK`) | true | pipeline output, or fallback-driven |
| Unavailable | No usable sensor: lost mid-session, or sensors disabled in a release build (a configuration error) | false | 0 |

| From | Event | To |
|------|-------|----|
| (construct) | sensors enabled | Acquiring |
| (construct) | sensors disabled, debug build | Live (`FALLBACK`) |
| (construct) | sensors disabled, release build | Unavailable (configuration error) |
| Acquiring | first valid sample (after any resume settle) | Live (`SENSOR`) |
| Acquiring | no valid sample within `SENSOR_START_TIMEOUT` (counted from the first poll after the settle), any build | Live (`FALLBACK`) |
| Live (`SENSOR`) | invalid polls past the hold (F6) | Unavailable |
| Live (`SENSOR`), Unavailable | app backgrounded (rule 9) | Acquiring |
| Unavailable | valid sample | Live (`SENSOR`) |

Every other pair (including Live plus a valid sample, anything from Live with `FALLBACK`, and Acquiring plus app backgrounded, which only clears the buffer and restarts the settle) is ignored: no state change, no error, no signal. `availability_changed(available: bool)` is emitted **whenever `valid` changes value**; the value at construction is not a change (so booting into `FALLBACK` or Unavailable emits nothing, and Acquiring to Live emits `true`, including Acquiring to Live `FALLBACK`). At construction `neutral_pending` and `neutral_stale` are true: the first `NEUTRAL_MIN_SAMPLES` valid samples capture the neutral (their median) and start the filter at 0, and `steer` is 0 until then.

**Run lifecycle (events, not states):** `run_reset` and `run_resumed` are capture events under rule 7; `run_paused`, `run_ended` and `run_started` change nothing in the core (the adapter uses `run_started` and every phase change to evaluate the level-triggered pause, rule 10).

### Interactions with Other Systems

All interfaces are **provisional** (only Run State and Tube Track have GDDs).

| System | Direction | Data / events | Interface owner |
|--------|-----------|---------------|-----------------|
| Ball Movement | out | pulled `steer` in `[-1, 1]` and `valid`, once per step; working assumption: `steer` maps to the ball's angular position (Open Question 10); Ball Movement owns gain, inertia and speed | Tilt Input owns the signal, Ball Movement the mapping |
| Run State & Restart | in / out | in: `run_reset` (with its previous phase), `run_resumed`, `phase_changed`, through a thin adapter owned by Tilt Input (Run State does not know Tilt Input); out: `pause_requested(sensor_lost)` whenever `valid` is false in Running or Resuming (on entry into those phases and on `availability_changed(false)`), through the same adapter; no Run State change is needed (it already accepts the source and a pause on the settling tick) | Run State defines the events and the pause source |
| Tube Track | convention | sign only: `steer > 0` raises `theta`; Tilt Input never calls Tube Track | Tube Track owns the frame |
| Platform Services | in | the portrait lock (project setting); the OS lifecycle notification, exposed as `app_backgrounded` / `app_foregrounded`; the Android sensor project settings; `app_interrupted` for Run State stays with Platform Services | Platform Services |
| HUD / Menus & Screen Flow | in / out | in: the touch half-screen hold, forwarded only while Running, feeding `fallback_source`; out: `valid` gates the Play and Resume buttons, `input_source` selects the "no motion sensor" notice, the sensor-lost pause screen offers the Menu | HUD, Menus |
| Settings & Accessibility | in (soft) | `sensitivity`, a divisor of `TILT_FULL_SCALE` (1 = default, larger = more sensitive), for players with limited wrist range | Settings owns the value |
| Pattern & Difficulty | indirect | the hazard-free start after a run begins covers a jolt that outlasts the settling tick (Run State Open Questions 11 and 17) | Pattern & Difficulty |

## Formulas

Tilt angles are in **degrees** (`asin` returns radians: convert when implementing). Every default value is in Tuning Knobs and is a guess until the on-device spike. Numbers were re-computed independently (Node) on 2026-09-21; nothing was run in Godot. Oracle values in the Acceptance Criteria use `dt = 0.016667` (a 16667 us tick), not `1/60`.

**Variables (shared)**

| Variable | Symbol | Type | Range | Description |
|----------|--------|------|-------|-------------|
| Gravity | g | Vector3 (m/s^2) | finite, `\|g\| >= G_MIN` | from `Input.get_gravity()`, screen-relative device axes; the raw sign convention is per platform, unverified |
| Roll | phi | float (deg) | [-90, 90] | left-right tilt; the right edge lower than the left is positive |
| Neutral | phi0 | float (deg) | +-`PHI_MAX` (70) | the captured pose |
| Relative, filtered | phi_r = phi - phi0, phi_f | float (deg) | +-(90 + 70) | before and after the filter |
| Step | dt | float (s) | [0, `DT_MAX`] | clamped clock difference (`DT_MAX`, the shared 0.1 s) |
| Filter gain | alpha | float | [0, 1) | filter coefficient |
| Effective full scale | FS_eff | float (deg) | 6-50 | see F4 |
| Steer | s | float | [-1, 1] | the published value |

**F1. Roll angle**

`phi = SENSOR_SIGN * deg(asin(clamp(g.x / |g|, -1, 1)))`

- Output [-90, 90]. A zero, NaN, infinite or below-`G_MIN` vector is rejected **before** F1. The clamp is needed because rounding can push `g.x / |g|` slightly above 1 (it lives in a helper `clamp_ratio` so it can be tested, AC-1).
- Example (pitch 30 deg, twist 30 deg): `g = (4.248, -7.358, -4.905)` gives `phi = asin(0.433) = 25.66` deg (iOS convention, `SENSOR_SIGN` +1).
- Noise gain is `1 / cos(phi)`: a 0.1 m/s^2 error is 0.58 deg at `phi` 0 but about 3.3 deg at `phi` 80, above the dead zone. Beyond about 70 deg the signal is unusable, and `asin` folds at 90 (tilting past it reads the same as tilting back): postures with `|phi|` above `PHI_MAX` are unsupported, which excludes lying on one's side.
- This is the geometric "right edge lower than the left" angle. A wrist twist about the screen normal loses gain as the phone is pitched back: a 50 deg twist reads 41.6, 22.5 and 3.8 deg at pitch 30, 60 and 85 (gravity cannot see the yaw part of a twist). Reaching `phi = FS_eff` by twist alone needs `sin(twist) = sin(FS_eff) / cos(pitch) <= 1`: at `FS_eff` 25 that is impossible above pitch 65 deg, and at pitch 60 it needs a 57.7 deg twist. Two fixes are deferred (Open Question 6): rotating `g` by `-phi0` about the screen normal before `asin`, and a blend with `atan2(g.x, -g.y)`.
- Vehicle acceleration adds an error of at most `asin(a / |g|)` to a raw accelerometer (11.8 deg at 2 m/s^2). `get_gravity()` is OS-fused, so the real figure is unknown, and fusion adds latency; the spike measures both.

**F2. Neutral capture**

`m = median{phi_i : t - G - W <= t_i <= t - G}` (closed interval; for an even count, the mean of the two middle values), `N` = the count, `spread = max - min` over the same samples. (`G` = `NEUTRAL_GUARD`, `W` = `NEUTRAL_WINDOW`, `N_min` = `NEUTRAL_MIN_SAMPLES`, `RA_F` = `REANCHOR_FRACTION`, `RA_S` = `REANCHOR_SPREAD`.)

- **Capture** (Menu or Boot reset, `run_resumed`, or a reset while `neutral_stale`): needs `N >= N_min`, otherwise `neutral_pending` (rule 8, the median of the first `N_min` valid samples afterwards).
- **Conditional re-anchor** (`run_reset` from Hit or Paused, not stale): recapture iff `N >= N_min` and `spread <= RA_S` and `|m - phi0| > RA_F * FS_eff`; otherwise inherit. The decision is the pure function `should_reanchor` (AC-16g).
- `phi0 = clamp(m, -PHI_MAX, +PHI_MAX)`. With `L_eff = max(0, PHI_MAX - FS_eff)`, when `|m| > L_eff` one `POSTURE_UNSUPPORTED` error is logged.
- Example: 18 samples in the window with median 3.4 gives `phi0 = 3.4`. The window is a closed interval: a 0.3 s window holds about 9, 18 and 30 samples at 30, 60 and 100 Hz (10, 19 and 31 when both ends align with a sample); every sample is a frame read (rule 4).
- **A rest pose beyond `L_eff` loses range, it does not add bias.** Rest at 60 deg with `FS_eff` 25 (`L_eff` 45): `phi0 = 60`, `steer` is 0 at rest, full lock toward the near side is intact, and toward the far side the roll can reach 70 deg, so `steer` reaches at most `(10 - 1.5) / 23.5 = 0.362`. That is the signal that the posture is unsupported.
- Constraints: `N_min <= floor(W * F_MIN)`, `G + W <= 0.9` and `SETTLE + G + W <= 1.0` (rule 14); the window fits inside the shortest resume countdown and the buffer.

**F3. Low-pass filter**

`alpha = 1 - exp(-dt / max(tau, TAU_FLOOR))`, `phi_f = phi_f + alpha * (phi_r - phi_f)`, `TAU_FLOOR = 0.005`.

- Start: `phi_f = 0` after every capture; `phi_f = phi_r` on entering Live from Acquiring or Unavailable without a capture; unchanged on an inherited neutral. A loaded config never has `tau < 0.02`; the `TAU_FLOOR` guard is defense in depth for direct `TiltMath` calls. Invalid polls do not update the filter; a valid sample uses the `dt` of its own poll (the stamp difference to the previous accepted poll, clamped).
- Example: `alpha = 0.2835` at dt = 0.016667 with tau = 0.05; 0.8647 at dt = 0.1. Gain 0.954, 0.847 and 0.303 at 1, 2 and 10 Hz; phase lag 48, 45 and 20 ms (continuous-time approximations; the exact oracles are AC-5 and AC-9).
- Extremes: dt = 0 gives alpha = 0 (holds); a NaN in `phi_f` is reset to 0 by the `BAD_OUTPUT` rule.
- **Noise rejection depends on the frame rate.** For an alternating +-A input the steady state passes `A * alpha / (2 - alpha)`: 0.33 A at 30 Hz, 0.165 A at 60 Hz, 0.083 A at 120 Hz (with tau fixed). AC-9 is therefore a 60 Hz claim: at 30 Hz the same +-4 deg input reaches `|phi_f|` 1.946 on the first tick, above the dead zone. V-2 measures the noise at the device's real frame rate; scaling tau with the frame rate is Open Question 25.

**F4. Dead zone, scale, clamp, response curve**

`FS_eff = min(FS / sensitivity, FS_EFF_MAX)`; `dz_eff = min(DZ, 0.2 * FS_eff)`;
`u = clamp(sign(phi_f) * max(0, |phi_f| - dz_eff) / (FS_eff - dz_eff), -1, 1)`; `s = sign(u) * |u|^k`.
(`FS` = `TILT_FULL_SCALE`, `DZ` = `DEAD_ZONE`, `k` = `CURVE_EXP`, `FS_EFF_MAX` = 50.)

- Output [-1, 1], continuous at the dead-zone edge (no jump, no hysteresis needed) and monotone for `k >= 1`. The denominator is at least `0.8 * FS_eff`, so at least 4.8.
- Example: `FS = 25`, `DZ = 1.5`, `phi_f = 14`, `k = 1.5` gives `u = 0.532` and `s = 0.388`.
- Extremes: `FS = 12` with `sensitivity = 2` gives `FS_eff = 6` and the dead zone caps at 1.2 (the dead zone shrinks as sensitivity rises while tremor noise in degrees does not, and the players who need high sensitivity are also the more tremor-prone: V-2 is re-checked at sensitivity 2). `FS = 45` with `sensitivity = 0.5` would need 90 deg for full lock; `FS_EFF_MAX` holds it at 50, and with `FS` 45 every sensitivity from 0.5 to 0.9 gives the same `FS_eff` of 50 (the slider range in Settings is defined against the shipped `FS`). `k >= 1` is required (`k < 1` amplifies noise).

**F5. Latency budget (design-time check, not runtime logic)**

`L_tilt ~ L_s + tau + T / 2` with `T` the frame time. A discrete filter with a zero-order hold lags a ramp by about `tau - T / 2`, so the sum is conservative by roughly half a frame; a sensor slower than the frame rate adds about half its period. `L_s` (sensor latency, including any OS fusion) is unmeasured: 30 ms is a placeholder and may be larger. The default `tau = 0.05` is checked against a target chosen after the spike (P-1); nothing in the running game evaluates this.

**F6. Sensor loss and the fallback input**

Consecutive invalid polls accumulate `invalid_us += min(dt_us, DT_MAX_us)` (integer microseconds, `dt_us` the stamp difference to the previous accepted poll) and `invalid_polls += 1`; a valid sample resets both. While `invalid_us <= HOLD_us` **or** `invalid_polls < DROPOUT_MIN_POLLS`: `steer` holds its last published value, `valid` stays true, the state stays Live. Otherwise the state is Unavailable, `valid` false, `steer` 0. Because each poll adds at most `DT_MAX`, a frame hitch of any length counts as at most `DT_MAX` and one poll, so a hitch plus one invalid sample never pauses the run (Run State's stall guard tolerates hitches up to 0.5 s). `HOLD = 0` is allowed: the third consecutive invalid poll then makes the state Unavailable, not the first.

- Example at 50 Hz (`dt_us` 20000, `HOLD` 0.1 s): the 5th invalid poll has `invalid_us` 100000 (not greater than 100000, still held), the 6th 120000 (Unavailable).
- Fallback: `s = move_toward(s, target, FALLBACK_SLEW * dt)` with `target` from rule 11; both keys or both halves give a target of 0. At `FALLBACK_SLEW = 4` per second, 0 to 1 takes 0.25 s.

## Edge Cases

**Sensor and samples**
- **If the chosen sensor's project setting is false on Android**: log one `SENSORS_DISABLED` error at boot naming the settings; the state is Live with `FALLBACK` in a debug build and Unavailable in a release build (a configuration error that must not ship). Tilt Input never silently steers 0 without that log line. On iOS the setting is not read.
- **If the vector is zero, NaN or infinite**: the sample is invalid; nothing reaches F1. A finite but huge vector may overflow `|g|` and give `phi` 0 (wrong but finite, no error; an upper bound is Open Question 7).
- **If `|g| < G_MIN` (free-fall, a thrown or dropped phone)**: the sample is invalid; rule 10 applies (hold, then Unavailable and a pause). The fused `get_gravity()` keeps a near-constant magnitude, so `G_MIN` is mostly inert there; on the raw accelerometer fallback a walking or bus jolt can trip it (V-8 measures spurious pauses).
- **If the platform reports gravity in g units** (`|g|` near 1): every sample is invalid (`G_MIN` 3), so the state times out to `FALLBACK` with one `SENSOR_TIMEOUT` error; both platforms deliver m/s^2, so the spike (V-5) confirms this once.
- **If the phone is accelerating (a braking bus, cornering, shaking)**: F1 uses the direction only; the error is unmeasured for `get_gravity()` (F1). Nothing detects it in the MVP.
- **If the sensor returns the same vector for a long time (stuck)**: not detectable by comparing vectors (the API repeats the last value by design); Open Question 7.
- **If the chosen source returns zero but another sensor returns valid data (no `TYPE_GRAVITY`, accelerometer present)**: the fallback order is decided in the sensor-source ADR; until then the state times out to `FALLBACK`.
- **If the screen is not in portrait at boot (the lock setting is missing)**: log one `NOT_PORTRAIT` error as a diagnostic; F1 still uses the screen-relative x axis, and the layout (not the sign) is what is wrong.
- **If there is no usable motion sensor at all (a phone without one, an emulator)**: after `SENSOR_START_TIMEOUT` the state is Live with `FALLBACK` in any build, with one `SENSOR_TIMEOUT` error; the touch half-screen hold steers, and the HUD shows the "no motion sensor" notice once.

**Neutral pose**
- **If the player restarts from Hit or Paused and the pose is unchanged or unsteady**: the previous `phi0` is inherited and the filter keeps running (a spread above `REANCHOR_SPREAD`, or a median within `REANCHOR_FRACTION * FS_eff` of `phi0`, means inherit).
- **If the player restarts from Hit or Paused and the phone was put down or moved to a steady pose more than `REANCHOR_FRACTION * FS_eff` away**: the window's median becomes the new `phi0`, the filter starts at 0 and no signal is emitted. **Risk:** a death while holding a steady large tilt looks the same; the ball then starts centered while the phone is tilted, and the player levels the phone against a fresh neutral (Open Question 22 measures how often).
- **If the window holds fewer than `NEUTRAL_MIN_SAMPLES` at a restart that is not stale (a long hitch)**: the previous `phi0` is inherited; the re-anchor never sets pending.
- **If the rest pose is beyond `L_eff`**: `phi0` is passed through up to `+-PHI_MAX` with one `POSTURE_UNSUPPORTED` error; `steer` is 0 at rest and the range on the far side is reduced (F2). A rest pose beyond `PHI_MAX` is clamped to it, and the relative angle is then not 0 at rest (rest 90 gives `steer` 0.787).
- **If fewer than `NEUTRAL_MIN_SAMPLES` fall in the window at a capture (the buffer was cleared by an app pause, or the run starts within about 0.55 s of launch)**: `neutral_pending` is set and the ball gets `steer = 0` until the first `N_min` valid samples have arrived, whose median becomes `phi0`. A jolt in those frames is filtered by the median, not eliminated (the guard does not apply on this path).
- **If `run_reset` from Menu or `run_resumed` arrives while the state is Acquiring or Unavailable**: `neutral_pending` is set; the capture runs on the first `N_min` valid samples and the filter starts at 0.
- **If `run_reset` from Hit arrives while `neutral_stale` is true (the app was backgrounded in Hit)**: it captures like a Menu-to-Play reset; if the player restarts soon after the settle the window may hold too few post-resume samples and the capture goes pending.
- **If the sensor is invalid when a run would start (a restart from Hit after a dropout, or a Restart on the sensor-lost pause screen)**: the run starts, the adapter's level trigger requests `pause_requested(sensor_lost)` on entry into Running, Run State pauses on the settling tick, and Resume stays disabled until `valid` is true; the pause screen offers the Menu.
- **If the resume countdown is interrupted by a pause**: `run_resumed` is not emitted (Run State), so no capture happens; the next `run_resumed` captures.
- **If two `run_resumed` events come in quick succession**: each capture is independent and uses the buffer at its own time.
- **If the app is backgrounded**: the buffer is cleared, `neutral_stale` is set and samples are discarded for `SENSOR_RESUME_SETTLE` after the foreground event. With Resume tapped at once, `run_resumed` comes at least 1.0 s later, and rule 14 guarantees `SETTLE + G + W <= 1.0`, so the window holds post-settle samples (AC-24). Only if the countdown is ever shortened below 1.0 s does this stop holding.
- **If the device sleeps**: `Time.get_ticks_usec()` is monotonic and unaffected by `time_scale`, but `CLOCK_MONOTONIC` on Android and `mach_absolute_time` on iOS may not count device sleep (UNVERIFIED, also Run State Open Question 5), so the clock gap after a screen-off can be small. The buffer clear of rule 9 is the protection against a stale window, not the age purge.

**Timing and polling**
- **If the clock stamp does not increase (equal, or going backwards)**: `dt = 0`, no sample is appended, the filter holds and `previous_now` is unchanged.
- **If a hitch is longer than `DT_MAX`**: `dt` is clamped for the filter and for the dropout counter; the ring buffer uses the real clock, so its windows are true time and samples older than `BUFFER_AGE` disappear.
- **If `poll` is called twice in one frame**: the second call carries the same clock stamp, so it appends nothing and uses `dt = 0` (rule 6 forbids it; this is the safe result).
- **If Ball Movement reads `steer` before `poll` in a frame (an ordering bug)**: it reads the previous frame's value, one frame stale; the game-loop ADR fixes the order and an integration test asserts it.
- **If the player taps the pause button and the phone jolts**: nothing special; Run State freezes the run on the same tick and the neutral is recaptured on resume.

**Mapping and settings**
- **If `phi_f` exceeds `FS_eff`**: `u` clamps to +-1 (full lock); there is no wrap and no overshoot.
- **If `sensitivity` from Settings is NaN, INF, zero or negative**: it is replaced by 1; a finite positive value is clamped to `[SENSITIVITY_MIN, SENSITIVITY_MAX]`; each replacement logs one `KNOB_CLAMPED`.
- **If `DEAD_ZONE` is at least 0.2 of `FS_eff`**: it is capped at `0.2 * FS_eff` (F4) at run time, on top of the load-time cap of `0.15 * FS`.
- **If both fallback directions are pressed**: the target is 0.
- **If sensors are live in a debug build**: the fallback input is ignored (`input_source` `SENSOR`).

## Dependencies

Tilt Input has **no hard dependencies** (Core layer: no upstream system needs to exist for it to work). It has soft data inputs, which are contracts and not code dependencies, and one hard dependent.

**Inputs (soft)**

| Input | Supplied by | Used for | Note |
|-------|-------------|----------|------|
| Motion sensors (`get_gravity()`, `get_accelerometer()`) | The device through Godot | F1 | Android registration is gated by `input_devices/sensors/enable_*` (default false, restart required); the iOS driver starts CoreMotion unconditionally (verified) and fuses the magnetometer (`XMagneticNorthZVertical`); desktop returns zero |
| `app_backgrounded` / `app_foregrounded` (provisional names) | Platform Services, from Godot's `NOTIFICATION_APPLICATION_PAUSED` / `_RESUMED` | Rule 9 | One owner per notification |
| `run_reset(run_id)` with its previous phase, `run_resumed(run_id)`, `phase_changed` | Run State & Restart, through a thin adapter owned by Tilt Input | Neutral capture, filter start, the level-triggered pause | Run State does not know Tilt Input (its rule 2); its rule 14 gives the ordering |
| The game clock in microseconds | The game loop (game-loop ADR, not yet written) | `dt`, ring buffer stamps | Shared `DT_MAX` clamp |
| Portrait lock (`display/window/handheld/orientation` = portrait) | Platform Services | Rule 3 | A project setting, not a runtime call |
| Touch half-screen hold (Running only) | HUD / Menus | `fallback_source`, rule 11 | Tilt Input reads no touch itself |
| `sensitivity` | Settings & Accessibility | F4 | Optional; default 1 |

**Dependents**

| System | Type | What it needs from Tilt Input |
|--------|------|-------------------------------|
| Ball Movement | Hard | `steer` in `[-1, 1]` and `valid`, once per step; the sign convention (`steer > 0` raises `theta`) |
| Run State & Restart | Soft | `pause_requested(sensor_lost)` from the adapter, level-triggered on `valid` in Running or Resuming |
| Menus & Screen Flow / HUD | Soft | `valid`, to disable Play and Resume while the sensor is lost; `input_source`, for the "no motion sensor" notice; a Menu path on the sensor-lost pause screen |
| Settings & Accessibility | Soft | The `sensitivity` hook |
| Playtest Telemetry | Soft (optional) | A `sensor_lost` pause counter and possibly steer statistics; not specified here |

**Bidirectional consistency**
- Run State & Restart lists Tilt Input as a soft dependent (`run_reset`, `run_resumed`) and, since 2026-09-21, the `sensor_lost` pause source (rule 10, requests table, events, AC-29), the sensor-lost pause screen and the Resume and Start gating (UI Requirements), and a reworded rule 14. The adapter learns the previous phase of a `run_reset` from the last `phase_changed` it saw, and its level trigger reads `phase_changed` too, so Run State needed no change (it already accepts a pause on the settling tick, its AC-22). Consistent.
- Tube Track defines the `theta` sign and does not depend on Tilt Input: consistent.
- To update in the systems index: row 3 (Tilt Input) wording (the source is `get_gravity()` pending the spike ADR); row 19 (Settings & Accessibility) already lists Tilt Input (soft: `sensitivity`); the Platform Services GDD must provide the portrait lock, the two lifecycle signals and the Android sensor settings (data inputs are not listed as dependencies in the index, as with Tube Track).
- Ball Movement's GDD must list Tilt Input as a hard dependency when it is written and state its `steer` mapping (Open Question 10); Menus & Screen Flow must gate Play and Resume on `valid` and offer a Menu path on the sensor-lost pause screen; HUD must forward the touch hold only while Running.

**Provisional assumptions:** how Ball Movement uses `steer` (position, per Open Question 10), the poll order in the game-loop ADR, the adapter contract with Run State, and the names of Platform Services' lifecycle signals.

## Tuning Knobs

All defaults are guesses until the on-device spike; none comes from keyboard testing. Every knob lives in a `TiltConfig` resource (data-driven), not in code. (`NEUTRAL_LIMIT` was removed in revision 2: a rest pose now passes through up to `PHI_MAX`, F2.)

| Knob | Default | Safe range | Affects | Too low | Too high |
|------|---------|------------|---------|---------|----------|
| `TILT_FULL_SCALE` (FS) | 25 deg | 12-45 | Wrist range for full lock (F4) | Tremor amplified, twitchy steering | Full lock unreachable by twist above about pitch 65 at 25 deg (a 50 deg twist at pitch 60 reads only 22.5 deg) |
| `DEAD_ZONE` (DZ) | 1.5 deg | 0-4 and at most 0.15 * FS | Rest steadiness (F4) | The ball never rests | Mushy fine control |
| `FILTER_TAU` (tau) | 0.05 s | 0.02-0.10 | Smoothness versus lag (F3, F5) | Tremor visible | Lag the player feels |
| `CURVE_EXP` (k) | 1.0 | 1-2 | Mid-range feel (F4); may move to Ball Movement (Open Question 20) | (below 1 is not allowed: amplifies noise) | Stiff mid-range, twitchy near full lock |
| `NEUTRAL_WINDOW` (W) | 0.3 s | 0.15-0.6 and `G + W <= 0.9` | Neutral robustness (F2) | One outlier decides | Window reaches earlier movement |
| `NEUTRAL_GUARD` (G) | 0.25 s | 0.05-0.35 | Skipping the tap jolt (F2); onset of a jolt precedes the handler time by the tap hold | Jolt leaks into the neutral | Window reaches earlier movement |
| `NEUTRAL_MIN_SAMPLES` (N_min) | 5 | 3-6 and at most floor(W * `F_MIN`) | Minimum frames in the window and in a pending capture (F2) | Outlier-prone | Always pending at low frame rates |
| `REANCHOR_FRACTION` | 0.5 (of `FS_eff`) | 0.3-1.0 | How far a steady restart pose must be from `phi0` to recapture (rule 7, F2) | Recaptures on small posture shifts, and on steady death tilts | Never recaptures a phone that was moved |
| `REANCHOR_SPREAD` | 3.0 deg | 1-6 | Steadiness needed for a recapture at restart (F2) | Tremor prevents any recapture | Any moving hand counts as a rest pose |
| `G_MIN` | 3.0 m/s^2 | 1-7 | Sample validity (rule 4) | Garbage direction in free-fall | Everything invalid (a platform reporting in g units is rejected entirely) |
| `SENSOR_START_TIMEOUT` | 2.0 s | 0.5-5 | Acquiring to Live or `FALLBACK` (states) | False `FALLBACK` on a slow start | Slow start on a phone with no sensor |
| `SENSOR_RESUME_SETTLE` | 0.3 s | 0.1-1.0 and at most `1.0 - G - W` | Samples discarded after an app foreground (rule 9) | A stale vector is accepted | Slow return to Live |
| `DROPOUT_HOLD` | 0.1 s | 0-0.3 | Accumulated clamped poll time of consecutive invalid polls before Unavailable, together with 3 polls (F6) | A blip pauses the run | Ghost steering |
| `FALLBACK_SLEW` | 4 per s | 1-20 | Fallback response (keys and touch) | Sluggish | Effectively digital |
| `SENSITIVITY_MIN` / `MAX` | 0.5 / 2.0 | fixed range of the Settings hook | `sensitivity` clamp (Edge Cases) | Very low sensitivity: `FS_eff` up to 50, harder full lock | High: `FS_eff` halves, dead zone shrinks, twitchy |
| `SENSOR_SIGN` | iOS +1, Android -1 (expected) | +1 or -1 | Direction of the roll (F1) | (set by V-1, not tuned) | (set by V-1, not tuned) |

**Fixed constants (not tuning knobs):** `TAU_FLOOR` = 0.005 s; `BUFFER_AGE` = 1.0 s and buffer capacity 256; `F_MIN` = 20 Hz (lowest supported frame rate); `PHI_MAX` = 70 deg (above it a posture is unsupported); `FS_EFF_MAX` = 50 deg; `DROPOUT_MIN_POLLS` = 3. **Set by the spike, not tuned:** `SENSOR_SIGN` per platform and `L_s`.

**Knob interactions**
- `G + W <= 0.9` and `SETTLE + G + W <= 1.0` (rule 14): inside the shortest `RESUME_COUNTDOWN` (1.0 s, Run State) and the buffer. A restart no longer always captures, so `RESTART_LOCK` does not enter.
- `NEUTRAL_MIN_SAMPLES <= floor(NEUTRAL_WINDOW * F_MIN)` (rule 14).
- `L_eff = max(0, PHI_MAX - FS_eff)` only decides when `POSTURE_UNSUPPORTED` is logged: raising sensitivity shrinks `FS_eff` and widens `L_eff`; lowering it does the opposite.
- `REANCHOR_FRACTION * FS_eff` scales with sensitivity: a more sensitive setting recaptures on a smaller shift.
- `DEAD_ZONE` and `sensitivity` interact through `FS_eff` (F4 caps the dead zone at `0.2 * FS_eff`).
- The wrist's comfortable range around a captured neutral is not measured (about 60-70 deg is an unverified guess).

**Sources of truth elsewhere:** `DT_MAX` (Run State and registry `dt_max`, 0.1 s), `RESUME_COUNTDOWN` (Run State), `sensitivity` (Settings & Accessibility), speed and angular response of the ball (Ball Movement).

## Visual/Audio Requirements

**Visual:** none. Tilt Input has no visuals of its own. How the ball responds on screen belongs to Ball Movement, Camera and Juice & Feedback; how a lost or missing sensor is shown belongs to HUD and Menus & Screen Flow. **Audio:** none. Haptic or sound feedback for near-misses belongs to Juice & Feedback and Platform Services. No `/asset-spec` is needed.

## UI Requirements

No player-facing UI of its own in the MVP. Requests to other systems:

1. **Settings & Accessibility:** a tilt sensitivity control that feeds `sensitivity` (range `SENSITIVITY_MIN` to `SENSITIVITY_MAX`, 0.5 to 2.0, default 1), useful for players with limited wrist range.
2. **Menus & Screen Flow / HUD:** (a) a one-time notice when `input_source` is `FALLBACK` in a release build ("No motion sensor found: hold the left or right side of the screen to steer"); (b) a "sensor lost" pause message, shown when the run pauses with `sensor_lost`, **with a way to the Menu** (never a dead end); (c) Play and Resume disabled while `valid` is false; (d) the touch half-screen hold, forwarded to Tilt Input only while Running and released on leaving Running, so it never clashes with tap-anywhere restart.
3. **No manual recalibrate control in the MVP.** If the spike shows posture drift needs one, it becomes a Pause screen or Settings action and a UX spec.

> **UX Flag:** this system has UI requirements. In Pre-Production, run `/ux-design` for the no-sensor notice, the sensor-lost pause message and the sensitivity setting before writing epics.

## Acceptance Criteria

Oracle-exact ACs are locked now for the pure math (F1, F3, F4) and for the capture and state logic, which do not depend on device values (the fixtures inject the config). Everything that depends on measured device behavior is a **spike exit criterion** (below) with a provisional hypothesis, not an AC.

Test targets: **[M]** `TiltMath` called directly (no config validation); **[C]** `TiltCore` with a validated `TiltConfig`; **[C\*]** `TiltCore` with the test-only unvalidated config; **[I]** integration test; **[L]** CI lint script (owner: a script in `tools/ci/`, not yet written; regex-based and best-effort). Unit tests live in `tests/unit/tilt_input/`, integration tests in `tests/integration/tilt_input/`; the framework (GUT or gdUnit4) is settled by the test-setup ADR (Open Question 16), and the ACs are framework-neutral (parameterized tests and signal counting exist in both).

**Fixtures.** Config: FS 25, DZ 1.5, tau 0.05, k 1, W 0.3, G 0.25, N_min 5, `REANCHOR_FRACTION` 0.5, `REANCHOR_SPREAD` 3.0, `G_MIN` 3, timeout 2.0, settle 0.3, HOLD 0.1, `FALLBACK_SLEW` 4, `sensor_sign` +1, sensitivity 1, `DT_MAX` 0.1, release build, `is_portrait` true, `sensors_enabled` true. `PHI_MAX` 70, `FS_EFF_MAX` 50, `F_MIN` 20, `BUFFER_AGE` 1.0 (capacity 256) and `DROPOUT_MIN_POLLS` 3 are constants, not config fields. `g(phi) = (9.81 sin phi, -9.81 cos phi, 0)`. Ticks at 60 Hz are **+16667 us (`dt` = 0.016667; every oracle below uses this `dt`)**; loss tests at 50 Hz (+20000 us). Tolerance 1e-3 deg and 1e-4 steer, exact for 0, states and counts. Times are integer microseconds; no test steps 60 Hz to reach a round second (120 ticks is 2.00004 s), tests inject explicit stamps. The first poll of a fresh core is at stamp 0 and is accepted (rule 6).
**Helper `live_core(pose, step_us = 16667)`**: builds a fresh core, feeds 60 polls at `pose` (stamps 0, step, 2 step, ...), then fires `run_reset` from Menu at the stamp of the last poll. Result: `phi0 = pose`, `phi_f = 0`, state Live, `neutral_pending` false, `neutral_stale` false; the next poll is at last + `step_us`. "After N ticks" means N polls after `live_core`. A test that needs a different starting state builds it explicitly.

**Roll and validity**
- **AC-1 [M]** (F1): `roll_deg` of (6,-8,0) is 36.870; (3,0,-4) is 36.870 (pitch-independent); (0,-9.81,0) is 0; (+-9.81,0,0) is +-90; (4.248,-7.358,-4.905) is 25.66 (+-0.01); `clamp_ratio(1.0000001)` is 1.
- **AC-2a [M]** (R4): `sensor_sign = -1` negates each AC-1 value. **AC-2b [C\*]:** with `live_core(0)`, a pose 10 deg above neutral gives `steer > 0` with sign +1 and `< 0` with -1 (sign plumbing only; the end-to-end check is AC-40).
- **AC-3 [C]** (R4): for each of zero, NaN, INF, (0,-2.99,0) and (0,-1,0), a fresh core in Acquiring given that vector keeps `sample_count` 0 and stays Acquiring; (0,-3,0) is accepted (`sample_count` 1, state Live).

**Filter and mapping**
- **AC-4 [M]** (F3): alpha(1/60, .05) = 0.283469, alpha(.1, .05) = 0.864665, alpha(0, .05) = 0; tau 0 or -1 floors to `TAU_FLOOR`: alpha(1/60) = 0.964326 (1e-6). **[C]:** `FILTER_TAU` 0 or -1 in a loaded config is clamped to 0.02 with one `KNOB_CLAMPED` error.
- **AC-5 [C]** (F3, R5): `live_core(0)`, constant pose 10: `phi_f` after 1, 3, 6 and 12 ticks is 2.8347, 6.3213, 8.6467 and 9.8169.
- **AC-6 [M]** (F4): k = 1: `phi_f` 1.5 gives 0 exactly; 0 gives 0; 14 gives 0.531915; 25 and 40 give 1; -14 gives -0.531915. k = 1.5: +-14 gives +-0.387939.
- **AC-7 [M]** (F4), one sub-case per line: **7a** sensitivity 2 with `phi_f` 7 gives 0.5; **7b** sensitivity 0.5 with `phi_f` 25.75 gives 0.5; **7c** FS 12, DZ 1.8, sensitivity 2 (`dz_eff` 1.2) with `phi_f` 3.6 gives 0.5; **7d** FS 12, DZ 4 (outside the config range, so `TiltMath` only), sensitivity 2 with `phi_f` 3.6 gives 0.5; **7e** FS 45, sensitivity 0.5 (`FS_eff` capped at 50) with `phi_f` 25.75 gives 0.5.
- **AC-8 [C]** (Edge): sensitivity NaN, INF, 0 or -1 behaves as 1 (`phi_f` 14 gives 0.531915) with one `KNOB_CLAMPED` error each; 3 equals 2 and 0.1 equals 0.5, each with one `KNOB_CLAMPED` error; 0.5 and 2.0 give none.
- **AC-9 [C]** (R5, a 60 Hz claim, see F3): `live_core(0)`, alternating +-4 deg from the first tick gives `steer` exactly 0 on all 120 ticks (max `|phi_f|` 1.1339, below the dead zone; steady state `4 * alpha / (2 - alpha) = 0.66056`). A weak noise test: it passes largely by attenuation; see AC-41 and Open Question 25.
- **AC-10a [C]** (R2): `live_core(0)`, 60 ticks at 10 deg give `steer` 0.361702; Acquiring and Unavailable give 0. **AC-10b [C\*]:** with FS = 0 (so `dz_eff` 0), a poll equal to the neutral gives `phi_f` 0 and F4 computes 0/0 = NaN: `steer` 0, `phi_f` reset to 0 and one `BAD_OUTPUT` error; the same core at `phi_f` 5 gives `steer` 1 and no error (an infinite quotient is clamped, only NaN is a bad output).

**Neutral**
- **AC-11 [M]** (F2): median of 7,1,3,80,5 is 5; of 1,2,3,4,5,80 is 3.5; nine samples at 2 and nine at 6 give 4.0.
- **AC-12 [C]** (R8): a fresh core fed 60 polls where the pose is 5 for every stamp up to `t - 250000` and 30 after, then `run_reset` from Menu at `t`: `phi0 = 5.000`, and the next poll at 5 gives `steer` 0. Repeat with -5 and -30: `phi0 = -5`.
- **AC-13 [C]** (R8): `live_core(15)` gives `phi0 = 15` and `steer` 0; `live_core(-15)` gives `phi0 = -15` and `steer` 0.
- **AC-14 [C]** (F2, closed interval; samples are appended in stamp order): four samples at 0 and four at 10 in the window (median 5). A ninth sample at 10 stamped `t - 550000` (appended first) or `t - 250000` (appended last) gives `phi0 = 10` (included); stamped `t - 550001` or `t - 249999` gives `phi0 = 5` (excluded). Each case is separate.
- **AC-15 [C]** (F2, pass-through), each case `live_core(pose)` then 120 ticks at the same pose: rest 35 gives `phi0 = 35`, `steer` 0 and no error; rest 60 gives `phi0 = 60`, one `POSTURE_UNSUPPORTED` error and `steer` 0, and a pose of 70 then gives a steady `steer` of 0.361702 (the far side loses range, the near side is intact); rest -60 gives `phi0 = -60`, one error; rest 90 (g = (9.81, 0, 0)) gives `phi0 = 70`, one error and a steady `steer` of 0.787234; with sensitivity 0.5 (`FS_eff` 50, `L_eff` 20) rest 35 gives `phi0 = 35`, one error and `steer` 0.
- **AC-16 [C]** (R7, capture policy), `live_core(5)` then 60 polls at the stated pose and `run_reset` at the last stamp (assert `phi_f` equal to its pre-event value where "not reset"): **16a** pose 12 (spread 0, offset 7 <= 12.5): from Hit and separately from Paused `phi0` stays 5, the filter is not reset and a steady `steer` is 0.234043; **16b** pose 20 (offset 15 > 12.5): from Hit and separately from Paused `phi0 = 20` and `phi_f = 0`; **16c** window alternating 20 and 22 (spread 2, nine each, median 21): recaptures `phi0 = 21`; alternating 20 and 24 (spread 4): inherits (the exact boundaries are tested on the pure function in **16g**, because `asin` output carries about 1e-14 of float error); **16d** `run_reset` from Menu at pose 6 and separately from Boot gives `phi0 = 6` (always captures, no threshold) and `run_resumed` at pose 6 gives `phi0 = 6`; **16e** after an app background, the foreground event and 1.0 s of polls at pose 12, `run_reset` from Hit captures (`neutral_stale` true) with `phi0 = 12`, and the flag is false afterwards; **16f** `run_reset` from Hit with a window holding four samples (not stale) inherits, `neutral_pending` stays false; **16g [M]** `should_reanchor(n, spread, median, phi0, fs_eff, n_min, ra_spread, ra_fraction)` with `n_min` 5, `ra_spread` 3.0, `ra_fraction` 0.5, `fs_eff` 25: offset exactly 12.5 gives false and 12.51 gives true; spread exactly 3.0 gives true (stable) and 3.01 gives false; `n` 4 gives false and 5 gives true; a negative offset of -12.51 gives true.
- **AC-17 [C]** (R8): with fewer than `N_min` samples in the window (an empty buffer, only samples younger than 0.25 s, only samples older than 1.0 s, or four in the window), a capture event sets `neutral_pending`; `steer` is 0; four further valid samples at 7 keep pending true and `steer` 0; the fifth gives `phi0 = 7`, `steer` 0 and pending false. Samples 7, 7, 90, 7, 7 give `phi0 = 7` (the outlier does not decide).
- **AC-18 [C]** (Edge): a capture event while the state is Acquiring or Unavailable sets pending; the capture takes the median of the first `N_min` valid samples (`steer` 0, `phi_f` 0), with exactly one `availability_changed(true)` from the state change.
- **AC-19 [C]** (R8): `live_core(5)` then N polls of 15: N = 1 gives `phi_f` 2.8347 and `steer` 0.056797; N = 2 gives 4.8659 and 0.143230; N = 3 gives 6.3213 and 0.205161 (a raw pass-through would give 0.361702). The number of polls before the first moving tick is asserted by AC-27.
- **AC-20 [C]** (R7): `live_core(0)`, holding 12 deg gives `steer` 0.446809 after 120 ticks and after 1800 ticks (difference below 1e-4: no recentering).
- **AC-21 [C]** (R8): two `run_resumed` events at `t` and `t + 1.5` s with the pose 5 until `t + 0.5` and 12 afterwards: the first `phi0 = 5`, the second `phi0 = 12` and `phi_f = 0`; two events at the same `t` give the same `phi0`.
- **AC-22 [C]** (lifecycle): `run_paused`, `run_ended` and `run_started` leave `phi0`, `phi_f`, state and sample count unchanged.
- **AC-23 [C]** (Edge): samples at 2 deg, a 10 s clock gap, then 0.6 s at 8 deg: `run_resumed` gives `phi0 = 8` and the buffer holds only post-gap samples (the age purge runs at every append; a synthetic clock jump, the real device-sleep behavior is Open Question 26).
- **AC-24 [C]** (R9): from Live, `on_app_backgrounded()` clears the buffer (`sample_count` 0), sets `neutral_stale`, moves to Acquiring and emits `availability_changed(false)`; from Unavailable it moves to Acquiring with no signal; in Acquiring it clears the buffer and restarts the settle with no state change and no signal. After `on_app_foregrounded()` at stamp F, polls at F + 299999 us append nothing and the state stays Acquiring, the poll at F + 300000 us is accepted (Live, `availability_changed(true)`, `phi_f = phi_r` because there was no capture), and the start timeout counts from the first post-settle poll. With Resume-equivalent `run_resumed` at F + 1000000 us the window holds post-settle samples (`N >= N_min`, `neutral_pending` false), covering the corrected "countdown refills the window" claim.

**Polling**
- **AC-25 [C]** (R6): a poll 500000 us after the previous one uses `dt = 0.1` (`phi_f` 8.6466 from a 10 deg step at `phi_f` 0, not 9.9995); the first poll uses `dt = 0`, is accepted at stamp 0 and appends its sample; a second poll with the same stamp appends nothing and leaves `phi_f` unchanged; the stamp sequence 100, 200, 150 (backwards, ignored, `previous_now` stays 200), 250 gives `dt` values 0, 100 us, 0 and 50 us.
- **AC-26 [C]** (R8): 600 samples at 1 ms stamps (a synthetic burst inside 0.6 s, distinct angles) leave `sample_count` at 256; 172 samples at pose A followed by 300 at pose B in the same burst and a `run_resumed` give `phi0` = B (the newest are kept, the oldest overwritten).
- **AC-27 [I], deferred, owner: the Ball Movement epic's loop story** (R6): the driver polls in Menu, Running, Paused and Hit; Ball Movement reads `steer` after `poll` in the same frame; exactly **2 polls** happen between a capture (`run_reset`) and the first tick with `dt_eff > 0` (the settling tick's poll and that tick's own poll), so the `steer` read on that tick equals the AC-19 N = 2 value for a 10 deg step at 60 Hz. Blocked on the game-loop ADR and the Ball Movement GDD.

**Availability, loss, fallback**
- **AC-28 [C]** (R9): `sensors_enabled = false` gives Live with `FALLBACK` (debug) or Unavailable (release), each with one `SENSORS_DISABLED` error and no `availability_changed`; `true` gives Acquiring with no error. **[I]:** the `TiltInput` node passes `sensors_enabled` from `ProjectSettings` on Android and `true` on iOS (needs a SceneTree; deferred).
- **AC-29 [C]** (states, explicit stamps): first poll at stamp 0 with no valid sample; at 1999999 us the state is Acquiring, `valid` false, `steer` 0; with no valid sample at 2000000 us (inclusive) the state is Live with `FALLBACK` (any build), with exactly one `SENSOR_TIMEOUT` error and one `availability_changed(true)`; a **valid** sample at exactly 2000000 us gives Live `SENSOR` instead (a valid sample wins); an every-poll vector of (0,-1,0) (g units) ends the same way as no sample.
- **AC-30 [C]** (R10, F6, at 50 Hz): `live_core(0, 20000)` then 50 ticks at 20.3 (`steer` 0.8), then invalid samples: through the 5th invalid poll (`invalid_us` 100000) `steer` stays 0.8, `valid` true, state Live, no signal (also for a single invalid poll); at the 6th (120000) the state is Unavailable, `valid` false, `steer` 0, `neutral_stale` true and exactly one `availability_changed(false)`, with no second signal on later polls.
- **AC-31 [C]** (R10): valid samples returning within the hold continue the filter (no restart; the valid sample uses one clamped poll `dt`, 0.02 s at 50 Hz, and the invalid polls did not move `phi_f`); from Unavailable, a valid sample gives Live, one `availability_changed(true)`, `phi_f = phi_r` and `neutral_stale` still true (no capture without a capture event); invalid polls advance `previous_now`.
- **AC-32 [C]** (states): table-driven over states {Acquiring, Live `SENSOR`, Live `FALLBACK`, Unavailable} and events {valid sample, invalid past the hold, start timeout, app backgrounded}: every pair not in the transition table (including Live plus a valid sample, Live with `FALLBACK`, and Acquiring plus app backgrounded, which changes no state and emits no signal but still clears the buffer, sets `neutral_stale` and restarts the settle) gives no change, no error and no signal.
- **AC-33 [C]** (R11, debug build, `sensors_enabled` false, one priming poll at stamp 0, then `dt = 1/16`): with `fallback_source` returning +1, `steer` is 0.25, 0.5, 0.75, 1.0 then holds; returning 0, it falls to 0 in 4 ticks; -1 gives -1; `valid` true. `fallback_source` is the same for keys and touch; a release build with a sensor timeout reaches the same behavior.
- **AC-34 [C]** (R11): sensors live in a debug build: the fallback source leaves `steer` unchanged and `input_source` stays `SENSOR`.
- **AC-35 [C]** (Edge): a non-portrait boot logs one `NOT_PORTRAIT` error and the `steer` for a 10 deg pose is identical with `is_portrait` true and false.
- **AC-36 [C]** (R14): table-driven, one row per knob **with its own fixture where the boundary depends on another knob**: a value below its range and above its range is clamped with one `KNOB_CLAMPED` error each; boundary values give none (DZ 4 needs FS >= 26.7, so its row uses FS 30; `N_min` 6 needs W >= 0.3); cross-knob rows: W 0.15 with `N_min` 5 gives `N_min` 3; DZ 4 with FS 12 gives DZ 1.8; `CURVE_EXP` 0.5 gives 1; G 0.35 with W 0.6 gives W 0.55; SETTLE 1.0 with G 0.25 and W 0.3 gives SETTLE 0.45; `sensor_sign` 0 or 2 gives the platform's expected value; a NaN or INF knob takes its default; `REANCHOR_FRACTION` and `REANCHOR_SPREAD` boundaries give none.
- **AC-37 [L]** (R1, R3, R11, R12, R13), five checks, each its own result (**37a-37e**), on `src/` only (`prototypes/` and `tests/` excluded), comments and strings stripped: **37a** the pattern `\bInput\.(get_(gravity|accelerometer|gyroscope|magnetometer)|get_joy_(gravity|gyroscope|accelerometer)|set_gravity)\b` matches only the `TiltInput` node file, and `Input.call(` and `Engine.get_singleton` do not appear in the tilt files; **37b** no `Input.`, `Engine.`, `Time.` or `OS.` in `TiltCore` or `TiltMath`, and neither is a Node; **37c** no `InputEventScreen*`, `InputEventMouse*`, `_input(`, `_unhandled_input(` or `Input.is_mouse_button_pressed` in the tilt files; **37d** `project.godot` has `display/window/handheld/orientation` = 1, the chosen Android sensor setting enabled (named in a constant list until the ADR) and the actions `steer_left` and `steer_right`; **37e** `TiltConfig.unvalidated()` is not referenced from `src/`. Best-effort: it cannot see aliasing such as `var i := Input`.
- **AC-38 [C]** (signals): `availability_changed` is emitted exactly when `valid` changes value: none at construction (including boot into `FALLBACK`, Unavailable or Acquiring); `true` on Acquiring to Live (`SENSOR` or `FALLBACK`); `false` and `true` around a loss.
- **AC-39 [I], deferred, owner: the adapter story** (adapter), sub-cases: **39a** `run_reset` from Menu triggers the capture synchronously before `run_started`, from Hit or Paused it conditionally re-anchors or inherits per AC-16; **39b** `run_resumed` captures; **39c** a pause during the countdown emits no `run_resumed`; **39d** `availability_changed(false)` during Running or Resuming becomes `pause_requested(sensor_lost)` and does nothing in other phases; **39e** a run that would start with `valid` false is paused by the level trigger (AC-43 is the pure-function part); **39f (UI type, manual walkthrough in evidence, ADVISORY)** Play and Resume are disabled while `valid` is false and the sensor-lost pause screen offers the Menu. Blocked on the real adapter and the Menus GDD; the Run State GDD already has the `sensor_lost` source (its AC-29).
- **AC-40 [I], owner: a story in the Ball Movement epic, created with that epic; the epic cannot close without it** (sign): with the right edge lowered, `steer > 0` and `theta` increases (blocked on the Ball Movement GDD). This is the end-to-end gate for the "wrong sign" failure; V-1 is its device counterpart.
- **AC-41 [I], deferred until the spike** (Pillar 2): recorded rest traces per posture (from V-2, as fixture files; integration only because it reads files) fed through `TiltCore` give `steer == 0` on at least 99% of ticks after the filter warm-up, for every trace whose peak-to-peak (V-2's metric) is at most `2 * DZ` (provisional numbers); a posture-drift trace is added. The `|theta|` drift criterion belongs to the Ball Movement GDD.
- **AC-42 [C]** (R12): the production sink lets at most one message per code per 1.0 s of injected clock through and does not let none through: one at t = 0; none at 999999 us; one at 1000000 us; two different codes at the same stamp both pass (counted independently); the level is preserved.
- **AC-43 [M]** (R10, level trigger): `sensor_lost_pause_needed(phase, valid)` is true for {Running, Resuming} with `valid` false, false for `valid` true, and false in Menu, Boot, Hit and Paused with either value.
- **AC-44 [C]** (F6): a single invalid poll stamped 0.2 s after the previous one (dt clamped to 0.1, `invalid_us` 100000, one poll) leaves the state Live and `valid` true; three consecutive invalid polls each 0.1 s apart (`invalid_us` 300000) give Unavailable on the third; with `HOLD = 0` the first and second invalid polls hold and the third gives Unavailable.
- **AC-45 [C]** (Edge): a fresh core has `neutral_stale` true and `neutral_pending` true, `steer` 0; `run_reset` from Boot then five valid samples give `phi0` from their median; both flags are false afterwards.
- **AC-46 [C]** (R2): reading `steer` twice without a poll returns the same value and emits no signal; a fed vector (1e200, 0, 0) gives a finite `steer` and no error.
- **AC-47 [C]** (R11): in a release build with `sensors_enabled = false` the state is Unavailable and the fallback source is ignored; after a timeout into `FALLBACK`, a late valid sample does not return to `SENSOR` (terminal); a constructor given an invalid Callable logs one error and leaves the core Unavailable.

**Gate policy.** (1) **V-1 signed device evidence** in `production/qa/evidence/tilt-input/v1-sign.md` is BLOCKING for the first playable. (2) **AC-40** has a named owner (a Ball Movement epic story, created with the epic) and is BLOCKING for that epic. (3) **AC-27 and AC-39** stay deferred and are tracked by their owner stories when those exist; they do not hold up Tilt Input stories. (4) The **[L] script** (`tools/ci/`) must exist before the first Tilt Input story is marked Done. (5) Test evidence follows the coding standards: [M], [C] and [C\*] are Logic (unit, BLOCKING); [I] is Integration (BLOCKING once its owner exists); AC-39f is UI (walkthrough doc, ADVISORY); [L] and the `project.godot` settings are Config/Data (smoke check, ADVISORY); spike items are prototype evidence under `production/qa/evidence/tilt-input/[id].md` (one file per criterion, with device, date, raw log and result).

**Spike exit criteria (provisional hypotheses, not ACs; every threshold is unmeasured).** Device matrix for all of them: an iPhone (current iOS and one older), a mid-tier Android (the target), a low-end Android without a gyroscope (where `TYPE_GRAVITY` is synthesized from the accelerometer) and a foldable or tablet if in scope. Tester counts are exploratory: report every result, do not average away a failure.
- **V-1 (BLOCKING for the first playable):** raw `g.x` sign per platform at the shipped orientation, with the right edge lowered about 30 deg (protractor or photo) and then the left edge, 5 trials each way per device, logged at 60 Hz, at least 2 Android vendors and 1 iPhone. Pass: the sign is the same in 5 of 5 trials per device and direction and opposite between the two directions; set `SENSOR_SIGN` per platform so F1 gives `phi > 0` for the right edge lowered (expected iOS +1, Android -1). The end-to-end check (right edge lowered, ball moves right, 10 of 10) is AC-40's device counterpart on the first playable build.
- **V-2:** rest noise (peak-to-peak and rms of `phi`) per pitch at 0, 30, 60, 85 and above 90 deg (phone held overhead, lying on the back) and side-lying, 5 testers for 60 s each; hypothesis: peak-to-peak at most `2 * DZ` at every supported pitch, re-checked at sensitivity 2 (the dead zone shrinks). Record where the formula stops working and how many players in the target audience play lying down.
- **V-3:** jolt amplitude and duration of the Menu-Play tap and the pause tap, at least 60 taps per tap type over at least 3 testers (59 taps without a failure support a 95% rate at 95% confidence); hypothesis: `|phi_f|` below `DZ` within `G` plus one frame for at least 95% of taps.
- **V-4:** feel: a 5-point lag scale (1 = not noticeable) at tau 0.02, 0.05 and 0.10 in a blind A/B, 5 testers; pass: no tester above 2 at the default tau; full lock reached in at least 90% of trials at pitch 30 and 60.
- **V-5:** reported units and `|g|` at rest; hypothesis: within [9.0, 10.6] m/s^2 on both platforms.
- **V-6:** (i) 30 s in the background, then foreground: the age of the last stale vector accepted must be 0 (the settle must discard it) and no `steer` above `DZ` may appear within `SETTLE`; (ii) posture drift: over a 10-minute session per tester, the median shift of the resting pose against `phi0` is at most 5 deg (more triggers Open Question 12 and 21); (iii) whether an iOS motion prompt or a compass-calibration overlay appears (none expected).
- **V-7:** raw `get_accelerometer()` versus `get_gravity()`: whether `TYPE_GRAVITY` exists on the target Android devices; latency difference; a vehicle test in a bus or tram with a reference accelerometer log of at least 2 m/s^2 lateral acceleration; hypothesis: `get_gravity()` error at most `2 * DZ`, with the raw accelerometer error recorded (expected up to 11.8 deg).
- **V-8:** spurious `sensor_lost`: zero pauses over 30 minutes of walking and 30 minutes of bus travel per tester, on the raw-accelerometer fallback device in particular; add a `sensor_lost` pause counter to Playtest Telemetry.
- **P-1:** end-to-end latency (`L_s` plus tau plus a frame) with a 240 fps camera, at least 100 samples on 3 devices, and whether gravity is pre-filtered; hypothesis: p95 at most 100 ms.
- **P-2:** sensor rate and `poll()` cost on the mid-tier Android target over at least 1000 frames; hypothesis: p95 at most 0.1 ms.
- **P-3:** behavior with the app in the background for 30 s: no steer spike, exactly one `availability_changed(false)` and one `true`.
- Visual and feel items need lead sign-off in `production/qa/evidence/`.

## Open Questions

| # | Question | Owner | Resolve when |
|---|----------|-------|--------------|
| 1 | (Resolved 2026-09-21) Pause on sensor loss is decided (`sensor_lost`, now level-triggered) and the Run State GDD has the source, the pause screen message and the Resume and Start gating; Menus & Screen Flow and HUD must implement the gating and the Menu path when authored | Menus & Screen Flow GDD | When authored |
| 2 | **On-device spike** (blocks locking every Tuning Knob): raw `g` and gyro log at full rate, postures at pitch 0 / 30 / 60 / 85 / above 90 and side-lying, several testers, iOS and Android; measure V-1..V-8 and P-1..P-3 | user (needs a phone) | Before the first playable and before Ball Movement tuning |
| 3 | Sensor-source ADR: `get_gravity()` versus accelerometer, the fallback order, whether `TYPE_GRAVITY` exists on target devices | technical-director, godot-specialist | Technical Setup |
| 4 | Game-loop ADR: one driver node in `_process` (with `process_mode` ALWAYS) calling `tilt.poll()`, then Run State's tick, then the ball step; not `_physics_process` (catch-up stamps); how the ball moves relative to physics interpolation (4.5) and 120 Hz frames (shared with Run State, its Open Question 4) | technical-director | Technical Setup |
| 5 | (Design half closed in revision 2) a rest pose beyond `L_eff` passes through to `PHI_MAX`; measure how often `POSTURE_UNSUPPORTED` fires | spike | After Q2 |
| 6 | F1 rework for side-lying and twist gestures at high pitch: rotate `g` by `-phi0` about the screen normal before `asin`, or an `atan2` blend weighted by pitch (`smoothstep(0.5, 0.9, cos pitch)`); until then those postures are unsupported (Player Fantasy) | spike, sensor-source ADR | After Q2 |
| 7 | An upper bound `G_MAX` for overflow; stuck-sensor detection cannot use vector equality (the API repeats the last value); a check would need an independent signal | spike, godot-specialist | After Q2 |
| 8 | (Closed in revision 2) the axes are screen-relative in every orientation (verified in source), so a sensor-driven portrait would not flip the sign; portrait stays locked for layout reasons, not for sign | | |
| 9 | iOS and Android motion access needs no privacy string or permission for device motion (platform docs, UNVERIFIED locally); confirm on a device that no prompt or crash occurs | Platform Services, export | Technical Setup |
| 10 | **Working assumption: Ball Movement maps `steer` to the ball's angular position** (game concept Core Mechanic 2, technical preferences), with a rate mapping as the fallback the spike compares; the prototype's 3.0 rad/s came from a keyboard and is a rate. Under a rate mapping a neutral error is continuous drift, which raises the cost of Open Questions 21 and 22 | Ball Movement GDD, spike | When that GDD is authored |
| 11 | How long the hazard-free start must be to cover a tap jolt that outlasts the settling tick (Run State Open Questions 11 and 17) | Pattern & Difficulty GDD | When authored |
| 12 | Manual recalibrate control: add only if the spike shows posture drift (V-6 ii) | UX, Settings | After Q2 |
| 13 | `sensitivity` hook range and default; larger full scale for limited wrist range | Settings GDD, accessibility-specialist | When authored |
| 14 | The prototype's left/right sign flip was never visually re-confirmed; V-1 settles `SENSOR_SIGN` | spike | After Q2 |
| 15 | `L_s` and the latency target are guesses; the default `FILTER_TAU` is checked after measuring (F5) | spike | After Q2 |
| 16 | Test framework: GUT (technical-preferences, this GDD) versus gdUnit4 (coding-standards CI line, `/smoke-check`); the ADR picks one, updates the CI line (the line `godot --headless --script tests/gdunit4_runner.gd` is not gdUnit4's documented entry, from memory) and runs `godot --headless --import` first; both are recent, 4.7.2 compatibility unverified for either; the AC-37 lint needs a CI script in `tools/ci/` | `/test-setup`, technical-director | Technical Setup |
| 17 | (Resolved) the F1 clamp branch is testable through the `clamp_ratio` helper (AC-1) | | |
| 18 | Mouse and web control are post-MVP (game concept); the development key fallback is not part of the release feature set | user | Full Vision |
| 19 | (Decided in revision 2) the touch half-screen **fallback is in the MVP**, only as the automatic no-sensor path (rule 11), not a selectable control scheme; an opt-in setting for limited wrist range or tremor stays open and belongs to Settings and accessibility | ux-designer, accessibility-specialist | Settings GDD |
| 20 | Move `CURVE_EXP` (and possibly `TILT_FULL_SCALE`) to Ball Movement, and define one end-to-end latency budget in one place; do not lock defaults before a joint on-device tuning session | Ball Movement GDD, spike | When that GDD is authored |
| 21 | How often an inherited neutral is wrong after a long Hit or pause with a posture change; the conditional re-anchor (rule 7) is the first answer, measure whether it suffices | spike, playtest | After Q2 |
| 22 | False re-anchor after a death: under a position mapping a steady large tilt is normal play, so a restart from Hit can adopt a death pose as the new neutral; measure how often, and if it matters restrict the re-anchor to restarts from Paused, or raise `REANCHOR_FRACTION` | spike, playtest, game-designer | After Q2 |
| 23 | Replace the `FALLBACK` key path by a debug-only node feeding `Input.set_gravity()` from the arrow keys (verified to exist), which would keep the core free of key handling and exercise the real pipeline; the touch fallback would remain | godot-specialist | When the core is implemented |
| 24 | Platform Services must own `NOTIFICATION_APPLICATION_PAUSED` / `_RESUMED` and expose `app_backgrounded` / `app_foregrounded`; delivery to a plain Node on iOS and Android is UNVERIFIED | Platform Services GDD | When authored |
| 25 | Noise rejection depends on the frame rate (F3): scale `FILTER_TAU` with the frame rate, or accept it; the spike measures noise at the real frame rate | spike | After Q2 |
| 26 | Whether the injected clock counts device sleep (Run State Open Question 5); rule 9's buffer clear is the protection either way | Run State, spike | After Q2 |
