# Review Log: tilt-input.md

## Review — 2026-09-21 — Verdict: MAJOR REVISION NEEDED
Scope signal: L (XL if the two named ADRs and the device spike are counted in the same work package)
Specialists: game-designer, systems-designer, qa-lead, godot-specialist; creative-director (senior)
Blocking items: 8 | Recommended: 9
Summary: The GDD was structurally complete (8/8 sections plus Visual/Audio, UI and Open Questions) and its core math checked out (F1, F3, F4 and every oracle re-computed). The review found that one of its three core mechanisms, neutral anchoring, produced a wrong neutral on the path the core audience uses most: the capture window before a restart always fell inside the frozen Hit phase (at the minimum lock it included the death frame), and the flat ±20 clamp turned a 35 deg rest pose into a permanent 0.574 steer bias. The sampling substrate also did not exist as described (`get_gravity()` has no timestamp, so the ring buffer held repeated frame reads), a stale sensor vector survives backgrounding on Android, a sensor dropout left the player with a dead control while the run continued (contradicting the GDD's own Pillar 2 claim), and no AC caught a wrong sign. Several ACs could not pass through a validated config (AC-4, 7, 10, 33, 43), and the "play anywhere" promise was wider than the roll formula supports (side-lying folds in `asin`).
Prior verdict resolved: First review

### Resolution (2026-09-21, same session)
All 8 blocking items were addressed in one revision pass. Decisions taken by the user (all four recommended options):
- Neutral policy: capture only at Menu-to-Play and at `run_resumed`; inherit the previous neutral on a restart from Hit or Paused; replace the flat clamp with a headroom clamp `L_eff = clamp(70 - FS_eff, 0, NEUTRAL_LIMIT)` and cap `FS_eff` at 50. The coupling to `RESTART_LOCK` is gone; the binding constraint is now the shortest resume countdown.
- Sensor loss: after a short hold, Tilt Input's adapter requests `pause_requested(sensor_lost)` (new Run State pause source); decay, limit and the Dropout state are deleted; Play and Resume are gated on `valid`.
- Scope cut: F5 became a design-time note, `UNITS_SUSPECTED`, `BAD_DT`, `TAU_ABOVE_CAP` and the mid-ladder capture were removed, log codes 9 to 6, states 5 to 3 (desktop keys became an input source). Oracle-exact ACs kept for the pure math and for capture and state logic; device-dependent items became spike exit criteria with provisional thresholds.
- "Play anywhere" scoped to seated, standing, reclined and steady-vehicle postures; side-lying and lying flat are unsupported until the F1 rework (Open Question 6).
Decisions taken from the review without a separate question: `dt` from the injected clock instead of a `real_dt` argument; the node handles `NOTIFICATION_APPLICATION_PAUSED` and `_RESUMED` (buffer cleared, settle window); `sensors_enabled` read from `ProjectSettings`; portrait named as `display/window/handheld/orientation` = 1 with no axis remap (verified in the 4.7.2 source); `get_gravity()` named as the working source and the 11.8 deg figure moved to the raw accelerometer; a sign-catching AC (AC-40, deferred) and a project-settings lint (AC-37); config validation order defined; `availability_changed` fires exactly when `valid` changes value.
Propagated the same day (approved by the user): Run State GDD (`sensor_lost` source, rule 14 and Tilt Input rows, pause screen message and gating, AC-29), the entity registry (`restart_lock` reference dropped, `dt_max` and `resume_countdown` comments), and the systems index (row 3 status In Review).
Deferred: the touch-steering fallback (Open Question 19, decide before MVP lock), moving `CURVE_EXP` to Ball Movement (Open Question 20), the F1 rework (Open Question 6), the on-device spike (Open Question 2), the two ADRs (sensor source, game loop) and GUT versus gdUnit4 (Open Question 16).
Status after revision: pending re-review in a fresh session (`/design-review design/gdd/tilt-input.md`).

## Review — 2026-09-21 (re-review) — Verdict: NEEDS REVISION
Scope signal: M (down from L; no new ADR, edits to four GDD sections plus the Pillar 4 wording in `game-concept.md` and a forward requirement on Platform Services)
Specialists: game-designer, systems-designer, qa-lead, godot-specialist; creative-director (senior). Three specialists hit an API rate limit on the first attempt and were re-run with the same prompts.
Blocking items: 5 | Recommended: 12
Summary: The math held: every oracle re-computed in Node and by hand, no NaN or division path. The remaining risk was lifecycle and test logic. (1) The `sensor_lost` pause was edge-triggered, so a restart from Hit with an already-invalid sensor ran on a dead control (found independently by game-designer and systems-designer). (2) The acceptance suite could not run against its own fixture (no base fixture, three ACs contradicted by the knobs, unspecified second-to-microsecond conversion). (3) The wrong-sign failure, a total loss in a one-axis game, had no gate and no owner. (4) Rate versus position contradicted the concept and the technical preferences. (5) The dropout hold used wall-clock time, so a frame hitch plus one invalid sample paused the run. The creative-director downgraded the verdict from MAJOR REVISION, confirmed the three-of-eight prior blockers as fully resolved, and adjudicated the specialist disagreements (below).
Prior verdict resolved: Partly. Fully resolved: sampling substrate, stale vector after backgrounding, ACs failing through the validated config. Partial at the start of this review, addressed in this revision: dead-window capture, flat clamp bias, dead control on dropout, wrong-sign AC, "play anywhere" overpromise.

### Resolution (2026-09-21, same session)
User decisions (all four recommended options): (1) conditional re-anchor at a restart from Hit or Paused (recapture only if the pre-tap window is steady, spread at most 3.0 deg, and its median is more than 0.5 x `FS_eff` from the neutral), amending the earlier pure-inherit decision; (2) Pillar 4 reworded in `game-concept.md` to "playable one-handed in any normal holding posture: seated, standing, reclined, or commuting"; (3) the touch half-screen fallback is in the MVP as the automatic no-sensor path only (`input_source` `FALLBACK`); (4) signed V-1 device evidence blocks the first playable and AC-40 has an owner in the Ball Movement epic.
Adjudicated by the creative-director and applied: level-triggered `sensor_lost` pause in Tilt Input's adapter (no Run State change); position mapping as the working assumption; a rest pose passes through up to `PHI_MAX` and `NEUTRAL_LIMIT` is removed (loses range, adds no bias); the margin `1.2 x FS_eff` was rejected as pre-spike tuning; portrait stays locked for layout reasons while the "axes flip" claim was corrected (source-verified: screen-relative in every orientation); Platform Services owns the lifecycle notification. Applied without a separate question: dropout hold by clamped poll time and at least 3 invalid polls; pending capture takes the median of `N_min` samples; `SETTLE + G + W <= 1.0` in rule 14; per-platform `SENSOR_SIGN` with validation; Android-only read of `sensors_enabled`; `process_mode` ALWAYS and no `_physics_process`; buffer 256; fixture helper `live_core`, `roundi(x * 1e6)`, corrected AC-10, 14, 29, 33, 34, 36, oracle numbers re-based on `dt` 0.016667, new AC-43..47, gate policy, V-1 protocol and V-6..V-8 thresholds, device matrix.
Propagated (approved by the user): `run-state-restart.md` (wording only, no behavior change), `entities.yaml` (`resume_countdown` comment), `systems-index.md` (row 3 status, Platform Services and HUD/Menus author notes), `game-concept.md` (Pillar 4).
Disagreements left visible: re-anchor versus pure inherit (the user chose re-anchor; false re-anchor after a steady death tilt is a known risk, Open Question 22); qa-lead's stricter gate (AC-27, 39 story IDs before Done) was narrowed by the creative-director to V-1 and AC-40.
New findings recorded, not fixed: noise rejection depends on the frame rate (at 30 Hz a +-4 deg alternating input reaches `|phi_f|` 1.946, above the dead zone; Open Question 25); the AC-27 oracle of 2 polls was derived from Run State rules 3 and 14 and needs confirming when the game-loop ADR exists.
Status after revision: pending a fresh-session re-review (`/design-review design/gdd/tilt-input.md`) and the on-device spike. Nothing has been run in Godot or on a device.
