# Review Log: platform-services.md

## Review — 2026-09-21 — Verdict: NEEDS REVISION
Scope signal: L (4 formulas, 7 dependents, 2 CI lints, 12 spike items; the game-loop ADR is still open)
Specialists: game-designer, systems-designer, qa-lead, godot-specialist; creative-director (senior)
Blocking items: 9 | Recommended: about 25 (most recorded as Open Questions or deferred to stories and the spike)
Summary: The spine held: the two-flag plus `focus_implies_suspend` lifecycle model, the injection seam, and every combinatorial claim (5460 and 340 sequence counts, the nine AC-1 rows, the signal invariants, the AC-3, F3 and F4 arithmetic) were re-derived with no mismatch. The blockers were localized contract holes, not a broken design. (1) F2's formula contradicted its own Extremes line and AC-3 for `now < last`. (2) The haptic gate could drop the HIT (the death cue) after a NEAR_MISS within the minimum interval, and on Android a later `vibrate()` replaces a running pulse, so "the caller owns priority" was unworkable (found independently by game-designer, systems-designer and godot-specialist). (3) There was no keep-screen-awake for a tilt-only game. (4) Player Fantasy promised that a pulled-down notification shade never costs a run while the Edge Cases said the run continues under it; Picture-in-Picture was undecided. (5) On Android (`focus_implies_suspend` true) a late `PAUSED/RESUMED` pair produced a phantom second interrupt. (6) No AC covered the `PlatformServices` node and AC-11 could not run without a settings reader. (7) The log sink contract was undefined. (8) PS-1..PS-4 had no pass criterion and the VIBRATE lint would skip forever because `export_presets.cfg` is gitignored. (9) Android 16 with target SDK 36 may not deliver the legacy Back path.
Prior verdict resolved: First review
Senior judgement [creative-director]: "a blocker must change the specification"; AC splitting, tolerance widths, lint robustness and story owners are story-stage work. Apply the 9 blockers and approve without a third full round; the next evidence is the harness and the device spike.
Disagreements left visible: touch-band pause for the shade (game-designer) versus sticky immersive mode (godot-specialist), adjudicated to immersive mode; grab-bag scope (game-designer) versus a platform facade that owns every OS touchpoint (creative-director), adjudicated as mechanism here and values elsewhere; AC-14 and AC-15 re-owned before approval (qa-lead) versus BLOCKING for the story only (creative-director).

### Resolution (2026-09-21, same session)
User decisions (all three recommended options): (1) the shade is mitigated by sticky immersive mode and the Player Fantasy no longer promises it (a top-edge touch-band pause was rejected); (2) the haptic gate uses a per-kind `priority` and a pulse-end condition, a strictly higher priority bypasses the gate (HIT over NEAR_MISS over UI_TAP), and drops are counted per cause; (3) no third full review round: approved-pending-harness-and-spike, next evidence is the `PlatformCore` harness and the device spike.
Applied without a separate question: F2 rewritten (`now < last` opens the gate; interval and pulse end both required); keep-screen-on added as Core Rules 12 (`screen_set_keep_on(true)` at boot, owned here); Picture-in-Picture disabled and immersive mode added to the AC-13 manifest; `PAUSED/RESUMED` ignored under `focus_implies_suspend` true (F1, the states table, AC-1 rows A2-A5, the AC-2 liveness check and an independent transition table); AC-11 rewritten as `[M]` on `PlatformSettings.mismatches`; new AC-19 (node forwarding, `fis_for_platform`) and AC-20 (keep-on at boot); `RateLimitedLog` with a defined key per code and sink signature `(level, code, key, msg)` (AC-6); PS-1..PS-4 pass criterion (a trial passes iff the trace equals a named AC-1 row) and named AC-1 rows; VIBRATE lint warns loudly, the `.gitignore` line goes with the first preset and an Android export with no preset fails; Android 16 / target SDK 36 Back requirement and PS-4 scope; header now "Supports Pillars 2 and 4"; AC-8 made ADVISORY; Open Question 7 resolved. Cheap recommended items also applied: NaN precedence in F3, a non-finite refresh rate in F4, real-time monotonic clock wording, `Callable` typing note, the fixture viewport 540x960, exact equality for `effective`, config validation order.
Deferred as Open Questions 19-25 (story stage or later): keep-on scope, OS audio policy owner, F3 continuity and other seam polish, AC hardening list, platform and layout follow-ups, Tilt Input settle on iOS FG without RET (cross-file edit, pending approval), the coding-standards device-spike evidence row.
Disagreements left visible: none new; the three from the review were adjudicated as listed above.
Propagated (approved by the user): `systems-index.md` (row 20 status and the Platform Services author note). Not touched: `tilt-input.md`, `run-state-restart.md` (Open Question 24 is the only pending edit).
Status after revision: In Review pending the `PlatformCore` harness and the device spike. Nothing has been run in Godot or on a device; the new AC-1 rows, the AC-2 invariants (10920 sequences, both `fis`, with liveness) and the AC-3 and AC-4 oracles were recomputed in a Node script with no mismatch (not yet in GDScript).

### Approval (2026-09-22)
The user accepted the revision without a third full document round (the Tilt Input precedent). Status: Approved, pending the `PlatformCore` harness and the device spike; `systems-index.md` row 20 and the progress tracker updated. No fresh-session re-review was run.
